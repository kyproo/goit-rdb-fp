# goit-rdb-fp — Final Project

**GoIT — Relational Databases**
**Student:** Kyrylo Proskurnia

## Project Structure

- `fp_queries.sql` — All SQL queries for tasks 1–5

## Tasks

1. Create `pandemic` schema, import `infectious_cases` data
2. Normalize `infectious_cases` to 3NF → `entities` + `infectious_cases_normalized`
3. Analyze `Number_rabies`: AVG, MIN, MAX, SUM — top 10 by avg DESC
4. Build year difference column using `MAKEDATE`, `CURDATE`, `TIMESTAMPDIFF`
5. Custom function `years_since(input_year)` — returns years since given year
